<section class="categories-index">

  <header class="page-header">
    <h1>Categories</h1>
  </header>

  <div class="categories">

    <% @categories.each do |c| %>
     	<article> <%= c.name %> </article>
    <% end %>

  </div>

  <%= link_to 'Create New Category', new_admin_category_path, class: 'btn btn-primary' %>

</section>
